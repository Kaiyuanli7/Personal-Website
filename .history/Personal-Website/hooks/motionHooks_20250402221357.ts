import { useScroll, useTransform, useSpring } from 'framer-motion'

// Export motion hooks as a default object to be used in dynamic imports
const motionHooks = {
  useScroll,
  useTransform,
  useSpring
}

export default motionHooks 